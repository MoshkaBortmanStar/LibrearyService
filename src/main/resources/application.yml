spring:
  application:
    name: LibrearyService
  datasource:
    url: jdbc:sqlite:src/main/resources/db/library_1.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
